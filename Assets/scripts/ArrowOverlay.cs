using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArrowOverlay : MonoBehaviour
{
    
    /**
    void Start()
    {
        //set south initially
        sprite = Resources.Load<Sprite>("Sprites/south_arrow");
        sprite = arrowSprite;
        SetArrowDirection(Vector2.down);
        raycastTarget = false;
         
        //transform.localPosition = new Vector3(0,0,-1);
    }

    public void SetArrowDirection(Vector2 direction)
    {
        //Calculate the angle from the direction vector
        float angle = Mathf.Atan2(direction.y, direction.x) * Mathf.Rad2Deg;

        //Set the rotation of the arrow Image
        rectTransform.rotation = Quaternion.Euler(0, 0, angle);
    }**/
}
